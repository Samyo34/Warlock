/**
 * Created by Matthias on 17/11/2016.
 */

/**
 * Class for the spell fireBall
 */
var fireBallCard = function(keyboardKey) {
    this.spellName = "fireball";
    this.keyboardKey = keyboardKey;
    this.cdProgress = 0;
};

/**
 * Class for the spell blink
 */
var blinkCard = function(keyboardKey) {
    this.spellName = "blink";
    this.keyboardKey = keyboardKey;
    this.cdProgress = 0;
};

/**
 * Class for the spell lightning
 */
var lightningCard = function(keyboardKey) {
    this.spellName = "lightning";
    this.keyboardKey = keyboardKey;
    this.cdProgress = 0;
};

/**
 * Class for the spell scurge
 */
var scurgeCard = function(keyboardKey) {
    this.spellName = "scurge";
    this.keyboardKey = keyboardKey;
    this.cdProgress = 0;
};